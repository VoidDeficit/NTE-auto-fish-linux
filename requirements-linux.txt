# Install these via pip
opencv-python-headless
numpy
dearpygui
pyautogui
jeepney
keyboard

# PyGObject (gi) must be installed via system package manager, NOT pip:
#   Arch/CachyOS: sudo pacman -S python-gobject
#   Ubuntu/Debian: sudo apt install python3-gi
# PyGObject >= 3.52 requires GLib 2.80+ which older distros don't have.
